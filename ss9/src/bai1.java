public class bai1 {
    // class
    //Class là một bản thiết kế (blueprint) để tạo ra các đối tượng.
    // Nó định nghĩa các thuộc tính (attributes) và hành vi (methods)
    // của một nhóm đối tượng.
        //+ Định nghĩa cấu trúc và hành vi chung cho các đối tượng.
        //+Class có thể là "con người" với các thuộc tính như cảm xúc, giác quan.
        //+class, method, attribute


    //Object
    //Object là thể hiện cụ thể của một class.
    // Nó chiếm giữ bộ nhớ và chứa trạng thái (state) và hành vi (behavior) xác định.
        //+Thể hiện và lưu trữ trạng thái cụ thể của class.
        //+object, instance, state, behavior
}
